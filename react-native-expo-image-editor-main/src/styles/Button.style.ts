// src/styles/Controller.style.ts

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  btn: {
    backgroundColor: '#0a9396',
    borderRadius: 24,
    paddingHorizontal: 24,
    paddingVertical: 12,
  },
  btnLabel: {
    color: '#fff',
    fontSize: 18,
  },
});

export default styles;
